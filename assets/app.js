const A=document.getElementById("app");
const pages={};
const code=s=>`<pre><button class="copy">Copy</button><code>${s.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;")}</code></pre>`;
const arch=`<div class="arch">
<div class="layer l5"><i class="n">5</i><div><b>Feature Modules</b><small>Flutter plugins · product features · native integrations</small></div></div><div class="down">↓</div>
<div class="layer l4"><i class="n">4</i><div><b>Core Orchestration</b><small>Flutter plugin · minimum runnable app · core flows</small></div></div><div class="down">↓</div>
<div class="layer l3"><i class="n">3</i><div><b>Component & Navigation</b><small>Flutter packages · routes · widgets · general UI contracts</small></div></div><div class="down">↓</div>
<div class="layer l2"><i class="n">2</i><div><b>Shared Layer</b><small>Flutter package · dependencies · repositories · models · utilities</small></div></div><div class="down">↓</div>
<div class="layer l1"><i class="n">1</i><div><b>Core Foundation</b><small>Flutter package · localization</small></div></div></div>`;
function wrap(ey,t,b,next,prev){return `<article class="article"><div class="ey">${ey}</div><h1>${t}</h1>${b}<div class="next"><span>${prev?`<a href="#/${prev[0]}">← ${prev[1]}</a>`:""}</span><span>${next?`<a href="#/${next[0]}">${next[1]} →</a>`:""}</span></div></article>`}
pages[""]=()=>wrap("CrusaderArch","Microservice architecture for scalable Flutter applications",`<p class="lead">CrusaderArch is an architectural approach for splitting a large Flutter application into independently developed, versioned and released packages and plugins.</p><div class="badges"><span class="badge">5 layers</span><span class="badge">Flutter packages</span><span class="badge">Flutter plugins</span><span class="badge">Independent releases</span></div><div class="buttons"><a class="btn primary" href="#/getting-started">Get Started →</a><a class="btn" href="#/architecture">Architecture</a><a class="btn" href="https://github.com/Mahfa/CrusaderArch" target="_blank">GitHub ↗</a></div><h2>Why?</h2><p>Large Flutter applications eventually accumulate navigation, authentication, networking, shared state, UI infrastructure, native integrations and product features. CrusaderArch introduces explicit boundaries so those responsibilities can evolve independently.</p><div class="grid"><div class="card"><b>Independent ownership</b><p>Each microservice has its own Git repository and release lifecycle.</p></div><div class="card"><b>Clear dependencies</b><p>Higher layers depend on lower layers; lower layers do not depend on features.</p></div><div class="card"><b>Feature isolation</b><p>Product-specific code and native integrations live in feature modules.</p></div></div><h2>The five layers</h2>${arch}<div class="callout"><b>The core idea:</b> lower layers define stable contracts; higher layers provide implementations.</div>`,["getting-started","Get Started"]);
pages["getting-started"]=()=>wrap("Get Started","Getting Started",`<p class="lead">The reference architecture uses Dart packages, Flutter plugins, semantic versioning and Cloudsmith as its package registry.</p><ol><li><b>Learn Dart package versioning.</b><br><a href="https://dart.dev/tools/pub/versioning" target="_blank">dart.dev/tools/pub/versioning ↗</a></li><li><b>Understand Flutter packages and plugins.</b><br><a href="https://docs.flutter.dev/packages-and-plugins/developing-packages" target="_blank">Flutter package/plugin guide ↗</a></li><li><b>Understand Cloudsmith.</b><br><a href="https://cloudsmith.io/" target="_blank">Cloudsmith ↗</a></li><li><b>Clone the reference project.</b>${code("git clone https://github.com/Mahfa/CrusaderArch.git")}</li><li><b>Study Layers 1–5 in order.</b></li><li><b>Replace the example prefix.</b> A project named <code>x</code> would use <code>x_localization</code>, <code>x_router</code>, etc.</li></ol><div class="callout warn"><b>Every microservice release:</b> update CHANGELOG.md → bump pubspec.yaml version → publish → push Git.</div>`,["architecture","Architecture"],["","Overview"]);
pages["principles"]=()=>wrap("Architecture","Core Principles",`<div class="grid"><div class="card"><b>One responsibility</b><p>Each service should have a clear reason to change.</p></div><div class="card"><b>Lower layers stay ignorant</b><p>Foundation/shared/navigation should not import product features.</p></div><div class="card"><b>Contracts before implementations</b><p>Routes/widgets can be declared below and implemented above.</p></div><div class="card"><b>Shared means shared</b><p>Do not turn the shared package into a feature-code dumping ground.</p></div><div class="card"><b>Packages vs plugins</b><p>The example uses packages for Layers 1–3 and plugins for Layers 4–5.</p></div><div class="card"><b>Every service is releasable</b><p>Repository, changelog, version and release belong to the service.</p></div></div><h2>Dependency rule</h2>${code("Feature → Base → Router / Widgets → Shared → Localization")}`,["architecture","Architecture"],["","Overview"]);
pages["architecture"]=()=>wrap("Architecture","Architecture Overview",`<p class="lead">CrusaderArch consists of five layers with intentional package/plugin boundaries.</p>${arch}<table><tr><th>Layer</th><th>Example</th><th>Type</th><th>Responsibility</th></tr><tr><td>1</td><td>example_localization</td><td>Package</td><td>Localization only.</td></tr><tr><td>2</td><td>example_shared_dependencies</td><td>Package</td><td>Common dependencies, repositories, models and utilities.</td></tr><tr><td>3</td><td>example_router / example_widgets</td><td>Packages</td><td>Navigation and UI contracts.</td></tr><tr><td>4</td><td>example_base</td><td>Plugin</td><td>Minimum runnable application and core flows.</td></tr><tr><td>5</td><td>example_battles</td><td>Plugin</td><td>Product-specific feature.</td></tr></table>`,["layer-1","Layer 1"],["","Overview"]);
pages["layer-1"]=()=>wrap("Layer 1","Core Foundation",`<p class="lead"><b>example_localization</b> is a Flutter package with one responsibility: localization.</p><h2>Implementation</h2>${code("Widget wrapInXLocalization(Widget child, {Locale? startLocale}) {\n  return EasyLocalization(\n    supportedLocales: const [Locale('fa'), Locale('en')],\n    path: 'resources/langs',\n    assetLoader: const CodegenLoader(),\n    fallbackLocale: startLocale ?? const Locale('en'),\n    startLocale: startLocale ?? const Locale('en'),\n    child: child,\n  );\n}")}<div class="callout"><b>Rule:</b> this microservice should handle localization and nothing else. The example uses easy_localization, but another implementation can be used.</div>`,["layer-2","Layer 2"],["architecture","Architecture"]);
pages["layer-2"]=()=>wrap("Layer 2","Shared Layer",`<p class="lead"><b>example_shared_dependencies</b> is a Flutter package containing infrastructure genuinely shared by other services.</p><h2>Examples</h2><div class="grid"><div class="card"><b>Dependency injection</b><p>GetIt and shared locator infrastructure.</p></div><div class="card"><b>Networking</b><p>HTTP/Chopper and API repository infrastructure.</p></div><div class="card"><b>State/data types</b><p>Shared data-state models and helpers.</p></div><div class="card"><b>Utilities</b><p>Device info, paths, debounce and common helpers.</p></div><div class="card"><b>Storage</b><p>Shared preferences and related infrastructure.</p></div><div class="card"><b>Platform utilities</b><p>Permissions, notifications and device-related utilities.</p></div></div><h2>Cloudsmith dependency</h2>${code("example_localization:\n  hosted:\n    url: https://dart.cloudsmith.io/crusader_example/crusader_example/\n    name: example_localization\n  version: ^1.0.0")}<div class="callout warn"><b>Do not dump features here.</b> Feature-specific models, repositories and BLoCs should normally remain inside the feature service.</div>`,["layer-3","Layer 3"],["layer-1","Layer 1"]);
pages["layer-3"]=()=>wrap("Layer 3","Component & Navigation Layer",`<p class="lead"><b>example_router</b> and <b>example_widgets</b> define reusable navigation/UI contracts without depending on higher-level feature implementations.</p><h2>Body Builder concept</h2>${code("Lower layer:\nexample_router\n     ↓\nRoute/page contract\n     ↓\nBody builder\n     ↑\nHigher layer:\nexample_battles")}<p>The router can know that a page exists without importing the feature that implements its body. The higher-level feature registers/provides the body.</p><h2>Widgets</h2><p>The widgets service can contain general UI with real bodies, such as error/message dialogs and snackbars, because these are reusable UI infrastructure rather than feature-specific pages.</p>`,["layer-4","Layer 4"],["layer-2","Layer 2"]);
pages["layer-4"]=()=>wrap("Layer 4","Core Orchestration",`<p class="lead"><b>example_base</b> is the minimum runnable application and is a Flutter plugin.</p><div class="grid"><div class="card"><b>Authentication</b><p>Login, registration and related identity flows.</p></div><div class="card"><b>Core account flows</b><p>Password recovery, verification and profile-related flows.</p></div><div class="card"><b>Home shell</b><p>The application scaffold that hosts features.</p></div><div class="card"><b>Startup</b><p>Application initialization and orchestration.</p></div><div class="card"><b>Updates</b><p>Core application update flow.</p></div><div class="card"><b>Native boundary</b><p>Plugin capabilities needed by base responsibilities.</p></div></div><div class="callout warn"><b>Base is not the feature layer.</b> Do not put every product feature into Base.</div>`,["layer-5","Layer 5"],["layer-3","Layer 3"]);
pages["layer-5"]=()=>wrap("Layer 5","Feature Modules",`<p class="lead"><b>example_battles</b> demonstrates a feature microservice. This is where most new product-specific microservices should live.</p>${code("example_battles/\n├── lib/\n│   ├── data/\n│   ├── domain/\n│   └── presentation/\n├── android/\n├── ios/\n├── pigeons/\n├── pubspec.yaml\n└── CHANGELOG.md")}<p>The example feature contains feature-specific models, repositories, BLoCs/pages/widgets, remote API code, native repositories and Pigeon APIs. Its plugin configuration defines Android and iOS implementations.</p><h2>Why plugins?</h2><p>A feature can own native Android/iOS code instead of pushing platform-specific implementation into the core application.</p>`,["microservices","Microservice Workflow"],["layer-4","Layer 4"]);
pages["microservices"]=()=>wrap("Workflow","Microservice Workflow",`<p class="lead">Each microservice has its own Git repository, version and release cycle.</p>${code("x_localization\nx_shared_dependencies\nx_router\nx_widgets\nx_base\nx_battles")}<h2>Release cycle</h2><ol><li>Change the service.</li><li>Update <code>CHANGELOG.md</code>.</li><li>Bump <code>version:</code> in <code>pubspec.yaml</code>.</li><li>Run generators/tests.</li><li>Publish the package/plugin.</li><li>Push the Git repository.</li></ol><div class="callout"><b>Important:</b> consuming services should depend on released versions, not copied source folders.</div>`,["cloudsmith","Cloudsmith"],["layer-5","Layer 5"]);
pages["cloudsmith"]=()=>wrap("Workflow","Cloudsmith & Publishing",`<p class="lead">The example uses Cloudsmith as its hosted Dart package registry.</p>${code("Git repository\n     ↓\nDart/Flutter package\n     ↓\nCloudsmith\n     ↓\npub dependency\n     ↓\nconsumer")}<h2>Publish target</h2>${code("publish_to: https://dart.cloudsmith.io/crusader_example/crusader_example/")}<h2>Consume a package</h2>${code("example_localization:\n  hosted:\n    url: https://dart.cloudsmith.io/crusader_example/crusader_example/\n    name: example_localization\n  version: ^1.0.0")}<p>Private registries require authentication. Keep tokens out of source control.</p><a href="https://docs.cloudsmith.com/formats/dart-repository" target="_blank">Cloudsmith Dart repository documentation ↗</a><div class="callout warn"><b>Cloudsmith is replaceable.</b> The architectural requirement is independently published/versioned services; the registry is an implementation choice.</div>`,["versioning","Versioning"],["microservices","Workflow"]);
pages["versioning"]=()=>wrap("Workflow","Versioning & Releases",`<p class="lead">Versions communicate compatibility between independently released services.</p>${code("1. Change code\n2. Update CHANGELOG.md\n3. Bump pubspec.yaml version\n4. Test / generate\n5. Publish\n6. Push Git")}<table><tr><th>Change</th><th>Typical movement</th></tr><tr><td>Bug fix</td><td>1.0.0 → 1.0.1</td></tr><tr><td>Backward-compatible feature</td><td>1.0.0 → 1.1.0</td></tr><tr><td>Breaking contract</td><td>1.0.0 → 2.0.0</td></tr></table><a href="https://dart.dev/tools/pub/versioning" target="_blank">Dart versioning ↗</a>`,["structure","Project Structure"],["cloudsmith","Cloudsmith"]);
pages["structure"]=()=>wrap("Reference","Project Structure",`<p class="lead">Each microservice should be independently understandable and releasable.</p>${code("x_feature/\n├── lib/\n├── android/       # when native Android is needed\n├── ios/           # when native iOS is needed\n├── pigeons/       # when Pigeon is used\n├── test/\n├── pubspec.yaml\n├── CHANGELOG.md\n└── README.md")}<h2>Feature internals</h2>${code("lib/\n├── data/\n│   └── repository/\n├── domain/\n│   ├── model/\n│   └── repository/\n└── presentation/\n    ├── bloc/\n    ├── page/\n    └── widget/")}<div class="callout"><b>Avoid a shared monolith disguised as packages.</b> If every feature still requires changing the same shared service, the boundaries are not giving you meaningful independence.</div>`,["body-builder","Body Builder Pattern"],["versioning","Versioning"]);
pages["body-builder"]=()=>wrap("Reference","Body Builder Pattern",`<p class="lead">This is one of the most distinctive ideas in CrusaderArch: lower layers can define what exists while higher layers provide how it is rendered.</p><h2>The problem</h2>${code("Avoid:\nexample_router → example_battles")}<h2>The solution</h2>${code("example_battles → example_router\n\nrouter defines contract\nfeature provides body")}<p>The router can define a page/route shell and delegate its body to a registered builder. The feature owns its implementation and registers it when the application is composed.</p><div class="grid"><div class="card"><b>Lower coupling</b><p>Router does not import every feature.</p></div><div class="card"><b>Feature ownership</b><p>Feature owns its UI and behavior.</p></div><div class="card"><b>Stable contracts</b><p>Lower layers can remain stable while features evolve.</p></div></div>`,["example","Example Application"],["structure","Project Structure"]);
pages["example"]=()=>wrap("Reference","Example Application",`<p class="lead">The reference project is named <code>example</code>, so its microservices use the <code>example_</code> prefix.</p>${code("example_localization\nexample_shared_dependencies\nexample_router\nexample_widgets\nexample_base\nexample_battles")}<h2>Example Battles</h2><p>The feature demonstrates feature-owned repositories, models, presentation code, native platform integration and Pigeon APIs.</p>${code("@HostApi()\nabstract class BattlesToNativeApis {\n  List<Battle> getBattles();\n}\n\n@FlutterApi()\nabstract class BattleFromNativeApis {\n  void battleSateChanged(String state);\n}")}<p>Its plugin configuration includes Android and iOS plugin classes, showing how a feature can own its native boundary.</p>`,["faq","FAQ"],["body-builder","Body Builder"]);
pages["faq"]=()=>wrap("Reference","FAQ",`<h3>Is CrusaderArch a framework?</h3><p>No. It is an architecture and reference implementation built with standard Dart/Flutter package and plugin mechanisms.</p><h3>Does every app need it?</h3><p>No. It is intended for larger, long-lived applications where team boundaries and independent releases justify the additional structure.</p><h3>Are these backend microservices?</h3><p>No. The term describes independently developed/versioned client modules. They are not the same thing as backend network microservices.</p><h3>Can I replace Cloudsmith?</h3><p>Yes. The registry is replaceable; independent publishing and versioning are the architectural requirements.</p><h3>Where do new features go?</h3><p>Normally Layer 5, as a new Flutter plugin repository.</p><h3>Can I rename example_?</h3><p>Yes. Replace the prefix with your project's name, for example <code>x_battles</code>.</p>`,null,["example","Example"]);
function render() {
  let k = location.hash.replace(/^#\/?/, "");

  if (!(k in pages)) {
    k = "";
  }

  A.innerHTML = pages[k]();

  document.querySelectorAll("nav a").forEach(x => {
    x.classList.toggle(
      "active",
      x.getAttribute("href") === "#/" + k
    );
  });

  document.querySelectorAll(".copy").forEach(b => {
    b.onclick = () => {
      const codeElement = b.parentElement.querySelector("code");

      navigator.clipboard.writeText(codeElement.textContent);

      b.textContent = "Copied";

      setTimeout(() => {
        b.textContent = "Copy";
      }, 900);
    };
  });

  document.getElementById("side").classList.remove("open");

  window.scrollTo(0, 0);
}
document.getElementById("search").oninput=e=>{let q=e.target.value.toLowerCase();document.querySelectorAll("nav a").forEach(a=>a.style.display=!q||a.textContent.toLowerCase().includes(q)?"":"none")};
document.getElementById("menu").onclick=()=>document.getElementById("side").classList.toggle("open");
document.getElementById("theme").onclick=()=>document.documentElement.classList.toggle("light");
addEventListener("hashchange",render);render();
